{
  "name": "api-gateway",
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "firebase": "^10.0.0",
    "node-fetch": "^3.0.0",
    "@vercel/node": "^2.0.0"
  }
}
